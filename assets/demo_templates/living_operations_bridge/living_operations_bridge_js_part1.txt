'use strict';
const STORY=JSON.parse(document.getElementById('axmStoryboard').textContent);
const IMPORT=JSON.parse(document.getElementById('axmImportReceipt').textContent);
const $=id=>document.getElementById(id),C=$('scene'),X=C.getContext('2d',{alpha:false});X.imageSmoothingEnabled=false;
const W=C.width,H=C.height,TAU=Math.PI*2,clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,v));
let playing=false,reduced=false,t=0,last=null,raf=null,cueIndex=0,viewMode='director',frame=0;
const CREW_ROLES=['engineering','science','navigation','command'];
const CUE_SECONDS=8;
function fmtMission(v){if(!Number.isFinite(Number(v)))return'T+UNKNOWN';const m=Math.round(Number(v)*60);return`T+${String(Math.floor(m/60)).padStart(2,'0')}:${String(m%60).padStart(2,'0')}`}
function fmtClock(s){return`${String(Math.floor(s/60)).padStart(2,'0')}:${(s%60).toFixed(1).padStart(4,'0')}`}
function hashSeed(text){let h=2166136261>>>0;for(const ch of String(text)){h=Math.imul(h^ch.charCodeAt(0),16777619)>>>0}return h>>>0}
function rng(seed){return()=>{seed=(Math.imul(seed,1664525)+1013904223)>>>0;return seed/4294967296}}
const R=rng(hashSeed(STORY.system_id||'AXM'));const stars=Array.from({length:110},()=>({x:R()*W,y:R()*H*.64,z:.25+R()*.75,s:R()<.1?2:1,p:R()*TAU}));
function cue(){return STORY.cues[Math.max(0,Math.min(STORY.cues.length-1,cueIndex))]}
function phase(){const p=((t%CUE_SECONDS)+CUE_SECONDS)%CUE_SECONDS/CUE_SECONDS;return p<.3?['command_outbound',p/.3]:p<.54?['observation_window',(p-.3)/.24]:['telemetry_return',(p-.54)/.46]}
function project(p,cam={x:0,y:0,z:0}){const z=Math.max(.6,p.z-cam.z+5),f=150/z;return{x:W/2+(p.x-cam.x)*f,y:H*.53-(p.y-cam.y)*f,s:f}}
function poly(points,fill,stroke=null){X.beginPath();points.forEach((p,i)=>i?X.lineTo(p.x,p.y):X.moveTo(p.x,p.y));X.closePath();if(fill){X.fillStyle=fill;X.fill()}if(stroke){X.strokeStyle=stroke;X.stroke()}}
function line(a,b,color,width=1){X.strokeStyle=color;X.lineWidth=width;X.beginPath();X.moveTo(a.x,a.y);X.lineTo(b.x,b.y);X.stroke()}
function text(str,x,y,color='#dff8ff',size=7,align='left'){X.fillStyle=color;X.font=`${size}px monospace`;X.textAlign=align;X.fillText(String(str),Math.round(x),Math.round(y));X.textAlign='left'}
function activityPulse(seed=0,speed=1){return reduced?.5:.5+.5*Math.sin(t*speed+seed)}
function activityTick(){return reduced?0:Math.floor(t*4)}
function ambientMode(){const [seg]=phase();return seg==='command_outbound'?'command':seg==='observation_window'?'science':'telemetry'}
function operationalFocus(){const [seg,p]=phase();if(seg==='command_outbound')return p<.34?'command':'navigation';if(seg==='observation_window')return'science';return p>.72?'command':'engineering'}
function receiptPattern(){const id=String(cue().outcome_id||'').toLowerCase();if(id.includes('quiet')||id.includes('constraint'))return'constraint';if(id.includes('clear')||id.includes('repeat'))return'repeatable';if(id.includes('unexpected')||id.includes('coupling'))return'coupling';if(id.includes('ambiguous'))return'ambiguous';return'neutral'}
function effectiveView(){if(viewMode!=='director')return viewMode;const [seg,p]=phase();if(seg==='observation_window')return'hologram';if(seg==='telemetry_return'&&p>.18)return'window';return'bridge'}
function crewPoseForRole(role,seed){if(reduced)return'still';const focus=role===operationalFocus();if(focus){const slot=Math.floor((t*1.05+seed*.41)%4);return['monitor','reach','reach','turn'][slot]}const slot=Math.floor((t*.42+seed*.73)%4);return['monitor','monitor','turn','monitor'][slot]}
function crewActivityState(){const focus=operationalFocus();return CREW_ROLES.map((role,i)=>({role,focus:role===focus,pose:crewPoseForRole(role,i+1),activity:Number((activityPulse(i+1,role===focus?1.8:1.05)* (role===focus?1:.68)).toFixed(3))}))}
function camera(){const mode=effectiveView(),director=viewMode==='director'&&!reduced,sway=director?Math.sin(t*.34+cueIndex)*.055:0,lift=director?Math.sin(t*.22+cueIndex*.7)*.025:0;if(mode==='window')return{x:sway,y:.3+lift,z:1.15};if(mode==='hologram')return{x:sway*.5,y:-.65+lift,z:.6};return{x:sway,y:lift,z:0}}
function drawStars(){X.fillStyle='#02040a';X.fillRect(0,0,W,H);const neb=reduced?0:t*.7;X.globalAlpha=.13;for(let i=0;i<7;i++){const nx=((i*91-neb*(.45+i*.04))%(W+150)+W+150)%(W+150)-75;X.fillStyle=i%2?'#47295d':'#123f5c';X.fillRect(Math.round(nx),34+i*11,96,3+i%3)}X.globalAlpha=1;for(const s of stars){const drift=reduced?0:t*(.12+.2*s.z);let x=(s.x-drift*7*s.z)%W;if(x<0)x+=W;const tw=reduced?1:.7+.3*Math.sin(t*.9+s.p);X.globalAlpha=(.35+.6*s.z)*tw;X.fillStyle=s.z>.7?'#dff8ff':'#7aa0b6';X.fillRect(Math.round(x),Math.round(s.y),s.s,s.s)}X.globalAlpha=1}
function drawPlanet(){const q=cue(),pulse=reduced?0:Math.sin(t*.28)*2,rot=reduced?0:Math.floor(t*5)%18;const mode=effectiveView(),cx=mode==='window'?150:112,cy=mode==='window'?98:82,rr=mode==='window'?55:42;X.fillStyle='#0b1d30';X.beginPath();X.arc(cx,cy,rr+pulse,0,TAU);X.fill();X.fillStyle='#1e5a64';X.beginPath();X.arc(cx-3,cy-2,rr-3,0,TAU);X.fill();X.fillStyle='#3d8a70';for(let i=-3;i<=3;i++){const bandX=cx-rr*.72+((i*13+rot)%(Math.max(18,Math.floor(rr))))-8;X.fillRect(Math.round(bandX),cy+i*5,Math.max(7,rr*.62),3)}X.fillStyle='#cde7dd';X.fillRect(cx-20+Math.floor(rot/3),cy-20,22,4);X.fillRect(cx+5-Math.floor(rot/4),cy+8,18,3);X.strokeStyle='#63e8ff88';X.strokeRect(cx-rr-3,cy-rr-3,(rr+3)*2,(rr+3)*2);text(q.target_planet_name||'TARGET',cx,cy+rr+12,'#78f3b4',7,'center')}
function drawWindow(){const top=24,bottom=190,left=42,right=438;X.fillStyle='#030912';X.fillRect(left,top,right-left,bottom-top);drawStars();drawPlanet();X.fillStyle='#090e16';poly([{x:0,y:0},{x:68,y:0},{x:92,y:30},{x:42,y:30},{x:0,y:62}],'#111823');poly([{x:W,y:0},{x:W-68,y:0},{x:W-92,y:30},{x:W-42,y:30},{x:W,y:62}],'#111823');poly([{x:0,y:H},{x:74,y:H},{x:118,y:190},{x:43,y:190},{x:0,y:218}],'#0d131c');poly([{x:W,y:H},{x:W-74,y:H},{x:W-118,y:190},{x:W-43,y:190},{x:W,y:218}],'#0d131c');X.fillStyle='#17202b';X.fillRect(0,0,W,10);X.fillRect(0,190,W,8);X.fillStyle='#314252';X.fillRect(0,10,W,2)}
function drawFloor3D(){const cam=camera(),nearZ=-2.2,farZ=5;for(let z=nearZ;z<=farZ;z+=.7){const a=project({x:-3.5,y:-1.4,z},cam),b=project({x:3.5,y:-1.4,z},cam);line(a,b,'#17303e',1)}for(let x=-3.5;x<=3.5;x+=.7){const a=project({x,y:-1.4,z:nearZ},cam),b=project({x,y:-1.4,z:farZ},cam);line(a,b,'#132936',1)}}
function box3d(cx,cy,cz,w,h,d,body,edge='#315365'){const cam=camera();const pts={};for(const dx of [-1,1])for(const dy of [-1,1])for(const dz of [-1,1])pts[`${dx},${dy},${dz}`]=project({x:cx+dx*w/2,y:cy+dy*h/2,z:cz+dz*d/2},cam);poly([pts['-1,1,-1'],pts['1,1,-1'],pts['1,-1,-1'],pts['-1,-1,-1']],body,edge);poly([pts['-1,1,1'],pts['1,1,1'],pts['1,1,-1'],pts['-1,1,-1']],'#101923',edge);return pts}
function console3d(x,z,side=1,mode='ops',role='engineering'){const bob=reduced?0:Math.sin(t*2+x)*.02;const focused=role===operationalFocus();box3d(x,-.78+bob,z,.95,.48,.66,focused?'#142738':'#111b26',focused?'#ffd078':'#315365');const p=project({x:x,y:-.56+bob,z:z-.36},camera()),tick=activityTick();X.fillStyle='#061118';X.fillRect(Math.round(p.x-15),Math.round(p.y-6),30,12);if(mode==='radar'){X.strokeStyle='#78f3b4';X.beginPath();X.arc(Math.round(p.x),Math.round(p.y),5,0,TAU);X.stroke();const a=reduced?0:t*2.2;line({x:p.x,y:p.y},{x:p.x+Math.cos(a)*5,y:p.y+Math.sin(a)*5},'#78f3b4',1)}else if(mode==='graph'){for(let i=0;i<6;i++){const h=2+Math.floor(activityPulse(i+x,1.5)*6);X.fillStyle=i%2?'#b9a5ff':'#63e8ff';X.fillRect(Math.round(p.x-13+i*4),Math.round(p.y+5-h),2,h)}}else{for(let i=0;i<6;i++){const blink=reduced?1:((tick+i+Math.floor(x*3))%4!==0);X.fillStyle=blink?(i%2?'#63e8ff':'#78f3b4'):'#18333f';X.fillRect(Math.round(p.x-13+i*4),Math.round(p.y-2+(i%2)*3),3,2)}}X.fillStyle=(tick+side)%5===0&&!reduced?'#ffd078':'#274858';X.fillRect(Math.round(p.x-13),Math.round(p.y+4),5,1);text(role.slice(0,3).toUpperCase(),p.x,p.y-8,focused?'#ffd078':'#55798a',5,'center')}
function crew3d(x,z,seed=0,seated=false,role='engineering'){const cam=camera(),pose=crewPoseForRole(role,seed),focused=role===operationalFocus(),bob=reduced?0:Math.sin(t*(focused?2.1:1.35)+seed)*.035,turn=reduced?0:Math.round(Math.sin(t*(focused?1.2:.72)+seed)*1.3),reach=pose==='reach'?(seed%2?1:-1):0,shift=focused&&!reduced?Math.sin(t*.72+seed)*.055:0;x+=shift;const body=project({x:x+(pose==='turn'?turn*.006:0),y:(seated?-.64:-.43)+bob,z},cam),head=project({x:x+turn*.012,y:(seated?-.18:.12)+bob,z},cam),sc=Math.max(1,body.s/16);X.fillStyle=focused?'#1c5187':'#173b66';X.fillRect(Math.round(body.x-4*sc),Math.round(body.y-10*sc),Math.round(8*sc),Math.round((seated?11:14)*sc));X.fillStyle='#24518a';X.fillRect(Math.round(body.x+(reach>=0?3:-7)*sc),Math.round(body.y-7*sc),Math.round((reach?6:4)*sc),Math.max(1,Math.round(2*sc)));X.fillStyle=seed%2?'#f0b272':'#c88762';X.fillRect(Math.round(head.x-3*sc),Math.round(head.y-3*sc),Math.round(6*sc),Math.round(6*sc));X.fillStyle=seed%2?'#e2c24d':'#5f2f2f';X.fillRect(Math.round(head.x-3*sc),Math.round(head.y-4*sc),Math.round(6*sc),Math.round(2*sc));if(!seated){X.fillStyle='#101722';X.fillRect(Math.round(body.x-4*sc),Math.round(body.y+3*sc),Math.round(3*sc),Math.round(6*sc));X.fillRect(Math.round(body.x+1*sc),Math.round(body.y+3*sc),Math.round(3*sc),Math.round(6*sc))}}
function hologram(){const cam=camera(),pattern=receiptPattern(),center=project({x:0,y:-.64,z:.9},cam),scale=Math.max(.55,center.s/27),spin=reduced?0:t*(pattern==='coupling'?1.25:.8),scan=reduced?.5:(.5+.5*Math.sin(t*(pattern==='ambiguous'?2.3:1.7)));X.globalAlpha=.12+.08*activityPulse(3,1.2);X.fillStyle='#28b9ff';X.fillRect(Math.round(center.x-2*scale),Math.round(center.y-34*scale),Math.max(2,Math.round(4*scale)),Math.round(36*scale));X.globalAlpha=.75;X.strokeStyle='#28b9ff';for(let i=0;i<4;i++){X.beginPath();X.ellipse(center.x,center.y,10*scale+i*8*scale,4*scale+i*3*scale,0,0,TAU);X.stroke()}for(let i=0;i<4;i++){const r=(12+i*7)*scale,a=spin+i*1.5;X.fillStyle=i