===2?'#ffd078':'#63e8ff';X.fillRect(Math.round(center.x+Math.cos(a)*r-1),Math.round(center.y+Math.sin(a)*r*.4-1),3,3)}X.strokeStyle=pattern==='repeatable'?'#78f3b4':pattern==='ambiguous'?'#ffd078':'#b9a5ff';X.beginPath();X.ellipse(center.x,center.y,36*scale*scan,13*scale*scan,0,0,TAU);X.stroke();if(pattern==='coupling'){X.beginPath();X.ellipse(center.x,center.y,28*scale,16*scale,spin*.18,0,TAU);X.stroke()}if(pattern==='ambiguous'){X.beginPath();X.ellipse(center.x-5*scale,center.y,24*scale,9*scale,.22,0,TAU);X.stroke();X.beginPath();X.ellipse(center.x+5*scale,center.y,24*scale,9*scale,-.22,0,TAU);X.stroke()}X.globalAlpha=1;text('MISSION MAP · '+pattern.toUpperCase(),center.x,center.y-26*scale,'#63e8ff',6,'center')}
function signal(){const [seg,p]=phase(),cam=camera();const mode=effectiveView(),ship=project({x:0,y:-.15,z:-.2},cam),target={x:mode==='window'?150:112,y:mode==='window'?98:82};let a,b,label,color;if(seg==='telemetry_return'){a=target;b=ship;label='TELEM';color='#78f3b4'}else{a=ship;b=target;label=seg==='observation_window'?'SCAN':'CMD';color=seg==='observation_window'?'#b9a5ff':'#ffd078'}const x=a.x+(b.x-a.x)*p,y=a.y+(b.y-a.y)*p;line(a,b,color+'66',1);X.fillStyle=color;X.fillRect(Math.round(x-2),Math.round(y-2),4,4);text(label,x+5,y-4,color,6)}

function drawResourceTelemetry(){const r=STORY.operations_context?.resources||{};const defs=[['reactor_reserve_percent','PWR','#63e8ff'],['sensor_health_percent','SNS','#78f3b4'],['hull_integrity_percent','HULL','#b9a5ff'],['fuel_percent','FUEL','#ffd078']];let y=38;for(const [key,label,color] of defs){const raw=Number(r[key]),known=Number.isFinite(raw),v=known?clamp(raw/100,0,1):0;X.fillStyle='#061018';X.fillRect(444,y,29,5);X.fillStyle=known?color:'#273945';X.fillRect(445,y+1,Math.round(27*v),3);text(label,440,y+5,known?color:'#55717f',5,'right');y+=10}const heat=Number(r.heat_percent);if(Number.isFinite(heat))text('HEAT '+heat.toFixed(1),443,83,'#ff9b73',5,'right')}
function drawOperationsPulse(){const ctx=STORY.operations_context||{},focus=operationalFocus(),pattern=receiptPattern();X.fillStyle='#02070bd9';X.fillRect(7,32,95,23);X.strokeStyle='#234b60';X.strokeRect(7,32,95,23);text('OPS '+focus.toUpperCase(),12,40,'#ffd078',6);text(`${ctx.open_thread_count??0} THREADS · ${ctx.available_action_count??0} ACTIONS`,12,49,'#7fa2b4',5);text(pattern.toUpperCase(),96,49,'#b9a5ff',5,'right')}

function receiptAttentionLights(){const mode=ambientMode(),color=mode==='command'?'#ffd078':mode==='science'?'#b9a5ff':'#78f3b4',tick=activityTick();for(let i=0;i<18;i++){const on=reduced?(i%5===0):((tick+i+cueIndex)%5===0);X.fillStyle=on?color:'#172731';X.fillRect(54+i*21,15,10,2);X.fillRect(54+i*21,187,10,2)}}
function ambientWash(){const mode=ambientMode(),pulse=reduced?.05:.035+.03*activityPulse(cueIndex+2,.9);X.globalAlpha=pulse;X.fillStyle=mode==='command'?'#ffb34f':mode==='science'?'#7d63ff':'#45efb2';X.fillRect(0,0,W,H);X.globalAlpha=1}
function drawBridge(){drawWindow();receiptAttentionLights();drawFloor3D();console3d(-1.9,.2,-1,'graph','engineering');console3d(1.9,.2,1,'radar','science');console3d(-2.45,1.7,-1,'ops','navigation');console3d(2.45,1.7,1,'graph','command');console3d(-.95,2.1,-1,'ops','navigation');console3d(.95,2.1,1,'radar','science');crew3d(-1.85,.45,1,true,'engineering');crew3d(1.85,.45,2,true,'science');crew3d(-.85,2.15,3,false,'navigation');crew3d(.85,2.15,4,false,'command');hologram();signal();drawOperationsPulse();drawResourceTelemetry();X.fillStyle='#0b1017';X.fillRect(0,198,W,72);for(let i=0;i<18;i++){const on=reduced?(i%6===0):((activityTick()+i)%5===0);X.fillStyle=on?(i%3===0?'#ffd078':'#63e8ff'):'#17323f';X.fillRect(12+i*11,214,7,3)}ambientWash();text('AXM LIVING BRIDGE / PRESENTATION ONLY',12,244,'#6f8f9e',6)}
function metric(k,v){return`<div class="metric"><span>${String(k)}</span><b>${String(v)}</b></div>`}
function updateUI(){const q=cue(),[seg]=phase(),crew=crewActivityState(),ops=STORY.operations_context||{},resources=ops.resources||{},actions=Array.isArray(ops.actions)?ops.actions:[],threads=Array.isArray(ops.open_threads)?ops.open_threads:[];$('phase').textContent=(playing?seg:'paused').replaceAll('_',' ');$('stationFocus').textContent=`FOCUS ${operationalFocus().toUpperCase()}`;$('target').textContent=`TARGET ${q.target_planet_name||'UNKNOWN'}`;$('missionClock').textContent=fmtMission(STORY.mission_time_hours);$('displayClock').textContent=fmtClock(t);$('receiptLabel').textContent=`TURN ${q.turn} · ${q.outcome_title||q.outcome_id||'event'}`;$('runtime').innerHTML=metric('renderer','living operations 3D')+metric('framebuffer','480×270')+metric('frames',frame)+metric('view',viewMode==='director'?`director → ${effectiveView()}`:viewMode)+metric('station focus',operationalFocus())+metric('pattern',receiptPattern())+metric('threads',ops.open_thread_count??'UNKNOWN')+metric('authority','presentation only');$('event').innerHTML=`<span class="tag info">${q.observation_truth_type||'unknown'}</span><p class="event">${q.observation||'No observation text.'}</p><span class="tag hold">${q.interpretation_truth_type||'unknown'}</span><p class="event">${q.interpretation||'No interpretation text.'}</p>`;$('facts').innerHTML=metric('light time',`${q.one_way_light_time_s??'UNKNOWN'} s`)+metric('reactor',Number.isFinite(Number(resources.reactor_reserve_percent))?`${Number(resources.reactor_reserve_percent).toFixed(1)}%`:'UNKNOWN')+metric('sensors',Number.isFinite(Number(resources.sensor_health_percent))?`${Number(resources.sensor_health_percent).toFixed(1)}%`:'UNKNOWN')+metric('hull',Number.isFinite(Number(resources.hull_integrity_percent))?`${Number(resources.hull_integrity_percent).toFixed(1)}%`:'UNKNOWN')+metric('event hash',(q.source_event_hash||'UNKNOWN').slice(0,12)+'…')+metric('world write','forbidden');$('actions').innerHTML=actions.slice(0,4).map((a,i)=>`<div class="opItem"><b>${i+1}. ${String(a.label||'Unnamed action')}</b><small>${String(a.category||'unknown')} · priority ${a.priority??'UNKNOWN'} · inspect only</small></div>`).join('')||'<div class="mini">No imported action menu.</div>';$('threads').innerHTML=threads.slice(0,6).map(th=>`<div class="threadItem"><b>${String(th.title||th.thread_id)}</b><span>${String(th.kind||'unknown')} · depth ${th.depth??'?'}</span></div>`).join('')||'<div class="mini">No imported thread summary.</div>';[...$('timeline').children].forEach((n,i)=>n.classList.toggle('active',i===cueIndex));window.__axmLowPixelBridge={schema:'axm.living-operations-bridge-runtime.v1',version:'0.3.0-candidate',playing,reducedMotion:reduced,displaySeconds:Number(t.toFixed(6)),frameCount:frame,cueIndex,viewMode,effectiveView:effectiveView(),segment:seg,ambientMode:ambientMode(),operationalFocus:operationalFocus(),receiptPattern:receiptPattern(),activityTick:activityTick(),crewActivity:crew,consoleActivity:'receipt_focused_presentation_only',hologramActivity:'receipt_pattern_presentation_only',directorMode:viewMode==='director'?'causal_choreography':'manual',resourceTelemetry:'read_only_source_snapshot',openThreadCount:ops.open_thread_count??0,availableActionCount:ops.available_action_count??0,sourceMissionTimeHours:STORY.mission_time_hours,sourceEventHash:q.source_event_hash,internalResolution:[W,H],authority:'presentation_state_only',mayAdvanceMissionTime:false,mayAppendEvent:false,mayRetargetEvent:false,mayModifyRuntimeResources:false,mayExecuteAction:false,mayCloseThread:false,mayChangeTruthLabels:false}}
function draw(){X.imageSmoothingEnabled=false;drawBridge();updateUI()}
function loop(ts){if(!playing){last=null;raf=null;return}if(last==null)last=ts;const dt=clamp((ts-last)/1000,0,.1);last=ts;t+=dt*Math.max(.1,Number($('speed').value)||1);frame++;draw();raf=requestAnimationFrame(loop)}
function setPlaying(v){if(v&&reduced){playing=false;draw();return false}playing=!!v;last=null;if(raf!=null){cancelAnimationFrame(raf);raf=null}if(playing)raf=requestAnimationFrame(loop);else draw();$('play').textContent=playing?'Pause animation':'Start animation';return playing}
function setCue(i){cueIndex=(Number(i)+STORY.cues.length)%STORY.cues.length;t=cueIndex*CUE_SECONDS;draw();return cueIndex}
function setView(v){const allowed=['director','bridge','window','hologram'];viewMode=allowed.includes(v)?v:'director';$('view').value=viewMode;draw();return viewMode}
function cycleView(){return setView(viewMode==='director'?'bridge':viewMode==='bridge'?'window':viewMode==='window'?'hologram':'director')}
function setReduced(v){reduced=!!v;$('reduced').checked=reduced;if(reduced)setPlaying(false);draw();return reduced}
function init(){X.imageSmoothingEnabled=false;$('timeline').innerHTML=STORY.cues.map((q,i)=>`<button type="button" data-i="${i}"><b>Turn ${q.turn}</b>${q.outcome_title||q.outcome_id||'event'}</button>`).join('');$('timeline').querySelectorAll('button').forEach(n=>n.addEventListener('click',()=>setCue(n.dataset.i)));$('play').addEventListener('click',()=>setPlaying(!playing));$('prev').addEventListener('click',()=>setCue(cueIndex-1));$('next').addEventListener('click',()=>setCue(cueIndex+1));$('view').addEventListener('change',e=>setView(e.target.value));$('speed').addEventListener('change',draw);$('reduced').addEventListener('change',e=>setReduced(e.target.checked));C.addEventListener('keydown',e=>{if(e.code==='Space'){e.preventDefault();setPlaying(!playing)}else if(e.code==='ArrowRight'){e.preventDefault();setCue(cueIndex+1)}else if(e.code==='ArrowLeft'){e.preventDefault();setCue(cueIndex-1)}else if(e.code==='KeyV'){e.preventDefault();cycleView()}});document.addEventListener('visibilitychange',()=>{if(document.hidden)setPlaying(false)});reduced=Boolean(window.matchMedia&&window.matchMedia('(prefers-reduced-motion:reduce)').matches);$('reduced').checked=reduced;draw()}
window.__axmLowPixelBridgeControl={setPlaying,setCue,setView,cycleView,setReduced,crewActivityState,ambientMode,operationalFocus,effectiveView,receiptPattern,draw};init();